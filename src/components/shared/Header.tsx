// Header is now a thin wrapper around the auth-aware Navbar.
// All pages that import Header will automatically get the correct sticky
// navbar with logged-in/out state.
export { default } from './Navbar';
