// Navbar is a fully client-side component that reads auth state
// from the browser Supabase client. This works in both server and
// client component contexts with no next/headers dependency.
export { default } from './NavbarClient';
